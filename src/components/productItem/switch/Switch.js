import './switch.scss'

const Switch = (props) => {
    const { switchStatus } = props
    return (
        <div className="switch">
            <div className="switch_circle"></div>
        </div>
    )
}

export default Switch
